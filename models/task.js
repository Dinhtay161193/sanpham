function Task(datepicker, task, idTask) {
  this.datepicker = datepicker;
  this.task = task;
  this.idTask = idTask;
}

/* class Task {
  datepicker;
  task;
  idTask;
  constructor(datepicker, task, idTask) {
    (this.datepicker = datepicker), (this.task = task), (this.idTask = idTask);
  }
}
 */